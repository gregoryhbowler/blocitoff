<% if current_user %>
  <h4>Add an item:</h4>
  <%= form_for [@user, @user.items.build], remote: true do |f| %>
    <%= f.label :body %>
    <%= f.text_field :body %>
    <%= f.submit %>
  <% end %>
<% end %>
